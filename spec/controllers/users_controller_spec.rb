RSpec.describe UsersController, :type => :controller do
  render_views

  describe 'GET /users' do 
    xit 'returns all users' do
    end
  end

  describe 'GET /users/:id' do 
    xit 'returns one user' do 
    end

    xit 'returns one user and their listings as a json attribute' do 
    end

    xit 'returns one user and their reservations as a json attribute' do
    end 

    xit 'returns one user and their hosts as a json attribute' do 
    end

    xit 'returns one user and their guests as a json attribute' do 
    end

    xit 'returns one user and their reviews from guests' do 
    end
  end

end